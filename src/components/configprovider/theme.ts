import {
  accentTextColor,
  backgroundColor,
  destructiveColor,
  secondaryBgColor,
  textColor,
} from "./variables";

export let tokenThemeConfig = {
  // Seed Token
  colorSuccess: accentTextColor,
  colorError: destructiveColor,
  colorBgBase: backgroundColor,
  borderRadius: 14,
  padding: 12,
  colorText: textColor, //
  colorBorder: secondaryBgColor, //
  lineWidth: 2, //
  fontFamily: "-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif, 'Apple Color Emoji', 'Segoe UI Emoji', 'Segoe UI Symbol'",
  fontSize: 14,
  // Alias Token
  //colorBgContainer: secondaryBgColor,
  }